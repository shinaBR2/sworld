.DS_Store
node_modules
.turbo
*.log
.next
dist
dist-ssr
*.local
.env
.env.*
.cache
server/dist
public/dist
.turbo
coverage/


# storybook
packages/ui/storybook-static/*
# generated by analyzer
apps/listen/stats.html


# Sentry Config File
.env.sentry-build-plugin

# Linear CLI local workspace config (may contain api_key)
.linear.toml

# Local Claude Code state — machine-local, never committed
# (memory/transcripts/global settings live in ~/.claude, outside the repo)
# settings.local.json holds the GH_TOKEN secret — ignore it in-repo, not only via a global gitignore
.claude/settings.local.json
.codegraph/
.claude/worktrees/
.claude/usage-tracking/
