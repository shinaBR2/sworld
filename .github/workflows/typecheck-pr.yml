name: Typecheck on PR

# Skips a PR whose every changed file lives in apps/hasura, .claude, or root
# markdown — the parts a workspace `pnpm typecheck` genuinely cannot reach:
# apps/hasura (SWO-549), .claude (skills/references/config — not a workspace
# package), and root-level docs like AGENTS.md/README.md.
#
# The filter lives in the `changes` job, NOT in `on:`. An `on: paths` filter
# stops the workflow triggering at all, so no check run is created and a
# required check sits pending forever, blocking the merge. Filtering in a job
# means the workflow always triggers: `changes` always reports, `typecheck` is
# skipped when nothing relevant changed, and GitHub counts a skipped job as a
# passing required check. Full note, including the picomatch glob-dialect trap
# when porting a filter, in e2e-main-pr.yml.
#
# Why the filter excludes hasura but NOT backend, despite SWO-549's title:
# `pnpm typecheck` is `turbo run typecheck --filter=!docs`, so it runs every
# workspace package exposing a `typecheck` script. Post-consolidation
# apps/backend is such a package (`tsc --noEmit`) and consumes `core` as a
# workspace dependency, so a backend-only change really can break this gate.
# This gate and unit-test-pr.yml are between them the backend's entire CI —
# biome.json excludes apps/backend from `pnpm lint` and no workflow lints it —
# so skipping backend here would gut half of it. apps/hasura is the opposite
# case: the package (`sworld-hasura-v2`) exposes only `lint` / `test:local` /
# `test:ci` and no `typecheck`, so turbo never visits it and this gate can only
# ever report an uninformative green for a hasura-only PR. That asymmetry is
# the whole filter.
on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # Cheap always-run gate: no checkout, reads the PR's changed files from the API.
  changes:
    # Explicit check name — see the note in hasura-pr.yml. Every path-filtered
    # workflow has a job called `changes`, and a check run is named after the
    # job, so without this two workflows report indistinguishable rows and
    # branch protection cannot tell them apart.
    name: typecheck path filter
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      typecheck: ${{ steps.filter.outputs.typecheck }}
    steps:
      - uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4.0.2
        id: filter
        with:
          # A negated pattern, on purpose: it matches any changed file outside
          # apps/hasura, .claude, and root-level markdown, so the output is false
          # only when *every* changed file is one of those. Written this way
          # rather than as a positive allow-list because an allow-list has to be
          # extended for every new app and every new root config file, and
          # forgetting silently reports green. A new path anywhere outside those
          # runs the gate by default.
          #
          # Root-level '*.md' (AGENTS.md, README.md, …) is documentation that
          # cannot break `tsc`, so a pure-docs PR should skip. It MUST be folded
          # into this one negated brace, NOT added as a separate `!*.md` line:
          # paths-filter OR's its patterns, and a standalone `!*.md` matches every
          # non-markdown file, which would force the gate to run on *every* PR —
          # including the apps/hasura- and .claude-only ones this filter exists to
          # skip. '*.md' is one path segment in the picomatch dialect, so it
          # catches root markdown only; a nested doc (apps/x/README.md) still runs
          # — deliberately conservative, since that's rare and running is harmless.
          #
          # The second pattern re-arms the gate for the one hasura-internal file
          # that can invalidate the assumption above: skipping is sound ONLY
          # while apps/hasura exposes no `typecheck` script for turbo to find.
          # Without it, the PR adding such a script would itself be the PR that
          # skips, and the post-merge run ignores apps/hasura too — nothing
          # would catch it until an unrelated PR went red.
          #
          # Note the re-arm is one-shot: it catches the manifest change, not the
          # hasura-only PRs that follow it. So if apps/hasura ever does gain a
          # `typecheck` script, delete this filter rather than trusting it.
          # .claude needs no such re-arm: pnpm-workspace.yaml is apps/* +
          # packages/* only, so nothing under .claude is a workspace package and
          # turbo can never visit it for a typecheck.
          filters: |
            typecheck:
              - '!{apps/hasura/**,.claude/**,*.md}'
              - 'apps/hasura/package.json'

  typecheck:
    needs: changes
    # Skip only on an explicit 'false' — i.e. the filter ran and said no. An `==
    # 'true'` test would also skip when `changes` itself errored, and a skipped
    # job scores as a pass, so an API blip would silently green the gate.
    if: ${{ !cancelled() && needs.changes.outputs.typecheck != 'false' }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.18.0'

      - name: Install pnpm
        uses: pnpm/action-setup@v4.0.0

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - uses: actions/cache@v3
        name: Setup pnpm cache
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install

      # Typechecks source across the frontend packages and apps/backend
      # (tests/stories/spec files are excluded via each package's
      # tsconfig.typecheck.json until SWO-351).
      - name: Typecheck
        run: pnpm run typecheck
