# WARP.md

This file provides guidance to WARP (warp.dev) when working with code in this repository.

**Read [`AGENTS.md`](./AGENTS.md).** It is the always-on repository context — the
scope, the mandatory gates, the working mindset, and pointers to where every
convention, command, and architecture detail actually lives.

This file deliberately holds no context of its own. It used to restate `AGENTS.md`,
and the copy drifted badly out of date. Don't refill it — one copy, one owner.
